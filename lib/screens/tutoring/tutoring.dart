import 'package:flutter/material.dart';
import 'package:task_5/screens/tutoring/tutoringview.dart';

class Tutoring extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: TutoringView());
  }
}
